# Kingly backend – FastAPI + ADK + Supabase
#
# Render builds were failing with pip "resolution-too-deep" due to the large dependency
# graph pulled by google-adk (and unconstrained version ranges). Pinning avoids resolver
# backtracking and makes deploys deterministic.
fastapi==0.133.0
uvicorn[standard]==0.41.0
python-dotenv==1.2.1

# NOTE: Avoid the heavy [extensions] extra on Render.
google-adk==1.25.1

supabase==2.28.2
PyPDF2==3.0.1
python-docx==1.2.0
google-generativeai==0.8.6
python-multipart==0.0.22
pyjwt==2.11.0
httpx==0.28.1
jwcrypto==1.5.6
